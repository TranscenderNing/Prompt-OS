# PromptOS 环境变量示例
# 复制为 .env 后 docker compose 会自动读取

# 生产模式对外端口（默认 8080）
PROMPTOS_PORT=8080

# LLM 默认配置（可选；前端也可在 UI 里每次填，二选一）
# 如果这里填了，没在前端配置的用户也能直接用后端默认 Key 调试
PROMPTOS_LLM_BASE_URL=
PROMPTOS_LLM_API_KEY=
PROMPTOS_LLM_MODEL=gpt-4o-mini
